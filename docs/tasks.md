# Задачи «shop» — roadmap и незакрытые пункты

> Статус: актуально на 24.09.2026 (по итогам аудита кода).
> Удалены устаревшие планировочные доки переноса с веба (`catalog.md`, `checkout.md`, `product-info.md`, `questions.md`) — фичи по ним реализованы (экраны `Catalog`/`Checkout`/`ProductInfo`/`Questions`). Актуальный роадмап — только этот файл.

## Сводная таблица

| # | Задача | Статус | Приоритет |
|---|--------|--------|-----------|
| P0-1 | Выход из аккаунта | открыто | P0 |
| P0-2 | Редактирование данных аккаунта | открыто | P0 |
| P0-3 | Кнопка «Отмена» в LoginByPhone — общий вид | открыто | P0 |
| P1-1 | Push-уведомления | открыто, обязательно | P1 |
| P1-2 | Онлайн-оплата | отложено до реализации бэка | P1 |
| P2-1 | Удалить мёртвый код (MovieDetails, LoginByEmail) | открыто | P2 |
| P2-2 | Убрать/проверить `console.*` | частично сделано | P2 |
| P2-3 | Доделать конфигурацию приложения (`app.json`) | открыто | P2 |
| P2-4 | Перезагрузка данных после входа (ремоунт) | сделано | P2 |
| P2-5 | `createNavigationContainerRef` на каждый рендер | сделано | P2 |
| P3-1 | Тесты критических путей | после визуальной готовности | P3 |
| F-1 | Зоны доставки на карте (+ выбор карты для Android) | открыто, требует исследования | Feature |

**Снято с повестки (решения пользователя):**
- Лист ожидания («сообщить о поступлении», P0-3 из аудита) — не делаем, назначение неочевидно.
- Повторный заказ («купить ещё раз») — не делаем как отдельный флоу; повтор доступен через карточку товара внутри заказа (добавление в корзину оттуда).
- Синхронизация корзины/избранного за пользователем (P1-8 из аудита) — остаётся локально (`AsyncStorage` persisted), как есть.
- Промокоды (P1-6 из аудита) — закрыто: скидки уже выводятся (скидки/промо в корзине и чекауте).

---

## P0-1. Выход из аккаунта (logout)

**Проблема:** в приложении нет кнопки «Выйти». Бэкенд-поддержка заложена (`auth/logout` в `src/shared/fetch-api/baseApi.ts:18` — для него берётся refresh-токен), но нигде не вызывается. Сейчас «выход» происходит только принудительно: 401 → `clearTokens()` → открытие login-модалки (`baseApi.ts:46-49`), при этом локальные стор-состояния не сбрасываются.

**Что сделать:**

1. **UI кнопки** в `ProfileScreen.tsx` (сейчас `navigateList` на стр. 107 не содержит выхода):
   - Добавить пункт «Выйти» или отдельную кнопку внизу списка (стиль — как вторичные кнопки проекта, см. P0-3).
   - Показать только для авторизованного пользователя (`phone` уже есть в стейте экрана).
2. **Подтверждение** — `Alert.alert` («Выйти из аккаунта?») или `BaseModal` (стиль `CancelOrderModal`).
3. **Логика выхода:**
   - `fetchService.post({ url: "auth/logout", payload: { refresh } })` — нужно получить refresh из `getTokens()` (`src/shared/storage/tokens.ts:15`).
   - `clearTokens()`.
   - Сброс локальных сторов: `basket`, `favorites`, `checkout`, `recent` (адаптеры лежат в `src/store/*`). Что именно сбрасывать — продумать: например корзину и чекаут — да, избранное — спорно (многие магазины хранят локально при госте), recent — оставить.
   - Перезагрузка/ремонт навигации — уже есть механизм: ремоунт через `key` на `RootStack.Navigator` (`src/navigation/Navigation.tsx:17`, `handleChangeKey`). Также закрыть login-модалку, если открыта (`modalsAdapter.closeLogin()`).
4. **Проверка поведения:** после выхода на холодном старте профиль должен снова показать `NotContent` «Войдите, чтобы продолжить»; повторный вход — рабочий флоу OTP.

**Проверка:** [ ] logout вызывает `auth/logout`, [ ] токены очищены, [ ] сторы сброшены, [ ] навигация обновлена, [ ] `lint` + `tsc`.

---

## P0-2. Редактирование данных аккаунта

**Проблема:** профиль только отображает телефон (`ProfileScreen.tsx:154-165`, `users/me`). Нет смены имени / телефона / почты.

**Что сделать:**

1. Уточнить на беке, какие поля поддерживает PATCH (`users/me`? отдельный эндпоинт?) — сейчас в коде только GET `users/me` (`ProfileScreen.tsx:80`).
2. Форма редактирования:
   - Паттерн проекта: `src/shared/ui/FieldInput` + zod-схемы (`src/shared/helpers/` уже есть `loginSchema.ts`, сделать `profileSchema.ts`).
   - Валидация по аналогии с `LoginByPhone`/`ReviewForm`; показ ошибок бэка через `getMessageError`.
   - Смена телефона, вероятно, с OTP-подтверждением (переиспользовать `LoginByPhone`-флоу по желанию — уточнить на беке).
3. После сохранения — обновить данные в `ProfileScreen` (перезапросить `users/me`).
4. UI: кнопка «Редактировать» в профиле → экран или модалка.

**Проверка:** [ ] сохранение полей, [ ] валидация + ошибки бэка, [ ] обновление в профиле, [ ] `lint` + `tsc`.

---

## P0-3. Кнопка «Отмена» в LoginByPhone — общий вид

**Проблема:** текущий стиль кнопки «Отмена» выглядит несистемно:
- `src/widgets/modal/login/LoginByPhone.tsx:377-398` — `buttonCancel: { backgroundColor: "lightgray" }`, текст `buttonTextCancel: { color: "#242424" }`, наследует высоту 48 и шрифт 600.

**Что сделать:**

1. Привести к общему вторичному стилю проекта:
   - Референсы: `src/widgets/modal/base-modal/BaseModal.tsx:160-170` (кнопка-отмена 36px/radius 12/серый текст) и `src/screen/OrderDetail/components/CancelOrderModal.tsx:91` (cancel с `backgroundColor: "#f6f6f9"`).
   - Палитра проекта: фон `#f6f6f9`, текст вторичный `#545454` (или `gray`/`#242424` — согласовать с макетом), fontWeight 700, borderRadius 12, высота 48 (в контексте модалки входа — под размеры основной кнопки).
2. Кнопка «Отмена» должна быть **отличима** от первичной «Войти» (`#a73afd`), но в той же сетке/высоте.
3. Если кнопок-состояний становится много — вынести общий компонент вторичной кнопки в `src/shared/ui` (но не обязательно: в проекте вторичные кнопки пока инлайновые).
4. Проверить обе формы входа (step 1 «Получить код» и step 2 «Войти»), а также disabled-состояние.

**Проверка:** [ ] визуально соответствует вторичным кнопкам проекта, [ ] контраст текста/фона корректный, [ ] `lint` + `tsc`.

---

## P1-1. Push-уведомления (обязательно)

**Проблема:** ни библиотеки, ни flow; в `package.json` ничего для пуша нет. Сценарии: статусы заказов (создан → подтверждён → в пути → доставлен), акции/промо.

**Что сделать:**

1. Выбрать стек:
   - Классика: `react-native-push-notification` + FCM (`@react-native-firebase/messaging`) или `notifee` (локальные + красивый рендер).
   - Android 13+: runtime permission `POST_NOTIFICATIONS`.
   - iOS: APNs + `userNotificationCenter`.
2. Уведомления по статусам заказа: при клике — deep link на `OrderDetail` (для этого понадобится связка: в заказе должен быть id; в навигации `OrderDetail` уже есть в `BasketStack`/`ProfileStack`, добавить в root при необходимости).
3. Бек: отправка device/registration token (`/devices` или подобный эндпоинт — уточнить), отправка пуша при смене статуса.
4. Локальный fallback: т.к. FCM требует настройки Firebase (google-services.json / GoogleService-Info.plist), можно начать с локальных уведомлений `notifee` и расширить позже.

**Проверка:** [ ] токен устройства уходит на бек, [ ] локальные уведомления работают (андроид/iOS), [ ] deep link открывает заказ, [ ] permission flow, [ ] `lint` + `tsc`.

---

## P1-2. Онлайн-оплата (отложено до реализации бэка)

**Проблема:** сейчас только `payment_method: "cash" | "card"` (обе — при получении), см. `src/store/checkout/types.ts:15`, `PaymentMethodCard.tsx`.

**Что сделать, когда бек реализует эквайринг:**

1. Добавить новый способ оплаты (СБП / банковская карта онлайн) в `checkout` store + `checkoutSchema.ts` (`z.enum` расширить).
2. Интеграция платёжного SDK/редиректа по схеме бэка; статус оплаты в `OrderModel` (`src/shared/types/order.ts:70` — `payment_method: string` уже строковый, расширится).
3. Экран «Оплатить заказ» / продолжение после создания заказа; обработка отмены/неуспеха платежа.
4. Отражение способа оплаты в `OrderCard`, `OrderDetail/components/DetailPaid.tsx`, `SuccessOrderModal`.

**Проверка:** по готовности бэка; [ ] создание заказа с онлайн-оплатой, [ ] возврат после платежа, [ ] отображение статуса.

---

## P2-1. Удалить мёртвый код

1. **`src/screen/MovieDetails/MovieDetailsScreen.tsx`** — заглушка (`props: any`, текст «MovieDetailsScreen»), нигде не импортируется и не зарегистрирована в навигации. Удалить папку `src/screen/MovieDetails/` целиком.
2. **`src/widgets/modal/login/LoginByEmail.tsx`** — не импортируется нигде (`LoginByPhone` самодостаточный, email-вход не вызывается). Решение: либо подключить «Вход по email/паролю» с переключением способа входа внутри модалки (нужна доработка хука/пропсов: сейчас `LoginByPhone` не предоставляет переключателя), либо удалить файл. По умолчанию — удалить, если email-вход не входит в scope.
3. Проверить после удаления: `npx tsc --noEmit` и `npm run lint`.

**Проверка:** [ ] файлы удалены, [ ] нет неразрешённых импортов, [ ] `lint` + `tsc`.

---

## P2-2. Убрать/проверить `console.*`

Осталось (проверить вручную):
- `src/screen/Checkout/components/CheckoutFooter.tsx:113` — `console.log(field)` в обработчике ошибок валидации. Удалить.
- `src/widgets/modal/login/LoginByPhone.tsx:132` — `console.warn("Не удалось определить код из SMS", err)`. Решить: показывать пользователю ошибку автоввода (сейчас просто warn) или оставить в dev-логах.

**Уже убрано (проверить, не вернулось):** `console.log("rerender check user")` в `BasketScreen`.

**Проверка:** [ ] `rg "console\\." src` — только допустимые dev-логи.

---

## P2-3. Доделать конфигурацию приложения (`app.json`)

**Проблема:** `app.json` — только `{ "name": "shop", "displayName": "shop" }` (46 байт).

**Что сделать:**

1. Иконки: `ios`, `android` (сгенерировать наборы: 1024×1024 и density-иконки).
2. Splash/launch screen (нативные настройки iOS/Android).
3. Версия/номер сборки: `version`, `buildNumber`; согласовать с отправкой в сторах.
4. `bundleIdentifier`/`package` — если используется Expo-конфиг или для пуша (/deep links), прописать ID.
5. Deep linking (schema + intent-filter) — понадобится для P1-1 (пуши → OrderDetail).
6. Проверить нативные папки: `android/`, `ios/` (app_name, display name) — какие значения сейчас прописаны.

**Проверка:** [ ] иконки/сплэш видны на обоих платформах, [ ] версия корректна, [ ] deep link открывает приложение.

---

## P2-4. Перезагрузка данных после входа (ремоунт) — сделано

Реализовано: `key`-ремоунт на `RootStack.Navigator` (`src/navigation/Navigation.tsx:11-21`), `Login` получает `rerender`; после успешного входа вызывается инкремент `key` → все экраны монтируются заново с токенами. TODO-блок из `Login.tsx` удалён.

**Осталось:** ручная проверка на устройстве — войти с телефона, убедиться, что подтянулись данные зарегистрированного пользователя (профиль, корзина, повторный вход после 401).

## P2-5. `createNavigationContainerRef` на каждый рендер — сделано

Убрано из `src/navigation/Navigation.tsx` (остался только `NavigationContainer` без локального рефа — реф не нужен, всё через ремоунт).

---

## P3-1. Тесты — после визуальной готовности

**Проблема:** покрытие минимальное — `src/store/checkout/adapter.test.ts` (адаптер чекаута) и `App.test.tsx` (снапшот).

**Что сделать (когда визуалка и флоу утвердятся):**

1. Юнит-тесты хранилищ/адаптеров: `basket`, `favorites`, `recent`, `modals`, `search` (паттерн — как `checkout/adapter.test.ts`).
2. Критические пользовательские пути (jest + react-test-renderer):
   - вход по телефону: фаза 1 (отправка OTP) → фаза 2 (ввод кода) → успех → закрытие модалки;
   - корзина → чекаут → создание заказа (мок `fetchService`).
   - выход из аккаунта (P0-1): сброс сторов.
3. По желанию e2e на Maestro/Detox после стабилизации UI.
4. Хелперы/схемы (`loginSchema`, `checkoutSchema`, `calcBasketInfo`) — уже частично покрыты (есть `getFullAddressItem.test.ts`, `adapter.test.ts`) — добить остальные.

**Проверка:** `npm test` зелёный + покрытие ключевых адаптеров.

---

## F-1. Зоны доставки на карте

**Требование (пользователь):** при оформлении доставки курьером нельзя выбирать любую точку — нужно рисовать зону доставки на карте. Параметры зон делаются на беке: у складов — радиус/полигон покрытия и стоимость (дальше от склада — дороже). Зоны настраиваются в админке, фронт динамически получает доступные зоны.

**Текущее состояние:** `CheckoutScreen` получает склады (`fetchWarehouses`, `CheckoutScreen.tsx:60`), карту рисует `src/screen/Checkout/components/map/MapBox.tsx` (`@rnmapbox/maps` v10.3.5). Сейчас можно выбрать любую точку; стоимость доставки фиксированная — `delivery_price = method_receipt === "courier" ? 100 : 0` (`CheckoutFooter.tsx:77`).

**Что сделать:**

1. **API (бек):** новый эндпоинт зон доставки (например `warehouse/zones` или поле в структуре склада): геометрия (полигон `GeoJSON` / центр + радиус), стоимость доставки по зоне, расписание. Уточнить модель данных в `src/shared/types/warehouse.ts` и `AddressItem`.
2. **Фронт — отрисовка зон:** в `MapBox.tsx` добавить `FillLayer`/`ShapeSource` с полигонами зон (или круг радиуса), подсветка зон, кликабельная область; при выборе точки не из зоны — блокировать выбор и показывать сообщение.
3. **Расчёт стоимости:** выбор зоны → `delivery_price` из зоны, отображение в `OrderSummary` и `CheckoutFooter` вместо фиксированных 100 ₽; учесть в `CreateOrderPayload`.
4. **Динамика:** подгрузка зон при входе в чекаут и при смене склада/способа получения; санитайзинг данных (зоны обновляются в админке).

**НЮАНС — карта не собирается под Android (блокер, исследовать в первую очередь):**
- Текущая: `@rnmapbox/maps`. Проблема: «не могу сбилдить под андроид с этой картой».
- Диагностика: версия `10.3.5` vs совместимость с React Native 0.87/Gradle; скачивание SDK-артефактов (Mapbox приватный репозиторий требует токен/регион); `MAPBOX_ACCESS_TOKEN` для Android (лимиты загрузки тайлов); network security / NDK / Java версия.
- Варианты замены (если не чинится): `react-native-maps` (Google Maps / Apple Maps), **MapLibre** (`@maplibre/maplibre-react-native` — drop-in замена Mapbox, без токенов) или web-view карта (хуже опыт). Выбор — отдельным прототипом: собрать минимальный Android build с каждой.
- Итог задачи: сначала прототип карты под Android, затем зоны доставки.

**Проверка:** [ ] aндроид-сборка с картой работает, [ ] зоны рисуются, [ ] выбор точки вне зоны заблокирован, [ ] стоимость зависит от зоны, [ ] `lint` + `tsc` + `jest`.

---

## Порядок работы (предлагаемый)

1. **P0** целиком: P0-1 (выход), P0-2 (редактирование), P0-3 (кнопка «Отмена»).
2. **P2** быстрые победы: P2-1 (мёртвый код), P2-2 (console.*), P2-3 (конфиг).
3. **F-1** — исследование Android-сборки карты → прототип → зоны доставки.
4. **P1-1** Push (после конфига и deep links), **P1-2** онлайн-оплата — по готовности бэка.
5. **P3-1** тесты — после стабилизации визуала и флоу.